import React from 'react'
import { View, Text } from 'react-native'

const OrdersScreen = () => (
	<View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
		<Text style={{ fontSize: 24 }}>Orders Screen</Text>
	</View>
)

export default OrdersScreen
